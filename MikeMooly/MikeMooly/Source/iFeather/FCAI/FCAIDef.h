#ifndef FCAIDEF
#define FCAIDEF

enum AI_STATE
{
    AI_STATE_PATROL,
    AI_STATE_STAY,
    AI_STATE_JUMP,
    AI_STATE_JUMP_MOVE,
    AI_STATE_ATTACK,
    AI_STATE_MAX,
};

enum PATROL_DIRECTION
{
    PATROL_DIRECTION_LEFT,
    PATROL_DIRECTION_RIGHT,
};

#endif
